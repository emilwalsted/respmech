# Pinned environment for producing/checking the RespMech golden reference.
#
# The current respmech.py / emg.py were written against an older scientific-Python
# stack. Two hard incompatibilities with modern scipy (>=1.14) mean the ORIGINAL
# code only runs faithfully on the versions below:
#   * scipy.integrate.cumtrapz was removed in scipy 1.14 (used for volume
#     integration from flow).
#   * scipy.integrate.simpson made `x` keyword-only in scipy 1.14 (respmech.py
#     and emg.py call it positionally).
# Use Python 3.12 with these pins to generate/verify the golden reference.
#
#   conda create -y -n rmgolden python=3.12
#   conda activate rmgolden
#   pip install -r tests/golden/requirements-golden.txt
#
numpy>=1.24,<2.0
scipy>=1.11,<1.14
pandas>=2.0,<2.3
matplotlib>=3.7,<3.10
seaborn>=0.12,<0.14
xlsxwriter>=3.1
openpyxl>=3.1
librosa>=0.10,<0.11
pytest>=7.0
